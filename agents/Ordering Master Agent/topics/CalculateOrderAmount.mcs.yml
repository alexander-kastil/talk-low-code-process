# Name: Calculate Order Amount
kind: AdaptiveDialog
modelDescription: Calculate the order amount
beginDialog:
  kind: OnRecognizedIntent
  id: main
  intent:
    triggerQueries:
      - plan an order
      - help me plan my order
      - order planning
      - how do I plan an order?
      - assist with planning my order
      - I want to plan an order
      - guide me through planning an order
      - can you help me organize my order?
      - set up an order plan

  actions:
    - kind: BeginDialog
      id: EEhrdu
      dialog: cr6f9_orderingMasterAgent.agent.Agent
      output:
        kind: SingleVariableOutputBinding
        variable: Topic.FoodData

    - kind: InvokeAIBuilderModelAction
      id: invokeAIBuilderModelAction_7vYSb8
      input:
        binding:
          Demand: =JSON(Global.orderDemand)
          FoodStockData: =JSON(Topic.FoodData)

      output:
        binding:
          predictionOutput: Global.foodToOrder

      aIModelId: 1d2c5135-265b-4f0b-a847-116d2ec700c5

    - kind: SendActivity
      id: sendActivity_u2mFDb
      activity: "You must order: {Global.foodToOrder.structuredOutput.result}"

    - kind: EndDialog
      id: g4ZGAA

inputType: {}
outputType: {}