# Name: Inventory Agent 
# Inventory Agent checks food inventory using a REST tool and computes an amount of food to order
kind: GptComponentMetadata
instructions: |-
  
  1) Ask the User for his demand using {System.Bot.Components.Topics.'cr6f9_inventoryAgent.topic.GetDemand'.DisplayName} 
  2) Get the the current stock {System.Bot.Components.Actions.'cr6f9_inventoryAgent.action.food-stock-connector-Getsallfood'.DisplayName} 
  3) Calculate the items to order using this formular: Demand + MinStock - InStock. Share your thoughts
  4) Convert the order items to an array {{ productName, requestedAmount }}
gptCapabilities:
  webBrowsing: true